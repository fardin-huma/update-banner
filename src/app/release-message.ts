/**
 * Optional release message to display in the update banner.
 * Set title and/or message to customize the notification.
 * Leave as null or empty strings to use the default generic message.
 */
export const releaseMessage: { title?: string; message?: string } | null = {
	title: 'Mandatory update: v0.1.0 is live',
	message: 'This minor release includes compatibility updates. Please refresh now to continue using the app.'
};
